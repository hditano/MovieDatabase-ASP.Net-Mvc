@model IAsyncEnumerable<Movie>

<h1>Hello Random Movie</h1>
<div class="card">
    <div class="card-body">
        @await foreach (var i in Model)
        {
            <p>@i.Title</p>
        }
    </div>
</div>